{
  "name": "operation-memory",
  "version": "0.1.0",
  "description": "Local-first operational memory CLI MVP",
  "type": "module",
  "bin": {
    "opmem": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "checkpoint": "bash scripts/checkpoint.sh",
    "smoke": "bash scripts/smoke.sh",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "verify": "bash scripts/verify.sh",
    "start": "node dist/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "yaml": "^2.8.4",
    "zod": "^4.4.3"
  }
}
